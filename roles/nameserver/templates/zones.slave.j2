
{% for zone in nameserver_zones %}
zone "{{ zone }}" {
        type slave;
        file "db.{{ zone }}";
        masters {
{% for master in nameserver_acl_masters %}{% if master != nameserver_query_source|default('') %}
                {{ master }};
{% endif %}{% endfor %}
        };
};

{% endfor %}

